# 獨立檔案 Log 功能說明

## 功能概述

獨立檔案 Log 功能會為每個下載的檔案建立專屬的 log 檔案，詳細記錄該檔案的下載過程、時間戳記及大小資訊，便於追蹤和除錯。

## 主要特色

- **獨立記錄**：每個檔案有自己的 `.log` 檔案
- **詳細資訊**：記錄遠端/本地的時間與大小
- **歷史追蹤**：採用附加模式，保留所有下載記錄
- **可選功能**：可透過設定檔啟用或停用

## 設定方式

### 在 config.properties 中啟用

```properties
# 為每個檔案產生獨立 log
separate_file_log=true

# 指定 log 目錄（選填，預設為 logs）
log_dir=./logs
```

### 設定說明

| 參數 | 說明 | 預設值 |
|------|------|--------|
| `separate_file_log` | 是否啟用獨立檔案 log | `false` |
| `log_dir` | Log 檔案存放目錄 | `logs` |

## Log 檔案格式

### 檔案命名規則

- **命名格式**：`原檔名.log`
- **範例**：
  - 下載檔案 `RYM01` → 產生 `RYM01.log`
  - 下載檔案 `TCD5920` → 產生 `TCD5920.log`

### Log 內容範例

#### 範例 1：下載新檔案

```
========================================
下載時間: 2024-01-15 14:30:25
遠端檔案時間: 2024-01-15 10:20:30
遠端檔案大小: 1024000 bytes
下載位元組數: 1024000 bytes
最終檔案大小: 1024000 bytes
本地檔案時間(下載後): 2024-01-15 10:20:30
下載原因: 本地檔案不存在
狀態: 下載成功
========================================
```

#### 範例 2：更新現有檔案

```
========================================
下載時間: 2024-01-16 02:15:10
遠端檔案時間: 2024-01-16 01:30:00
遠端檔案大小: 2048000 bytes
本地檔案時間(下載前): 2024-01-15 10:20:30
本地檔案大小(下載前): 1024000 bytes
下載位元組數: 2048000 bytes
最終檔案大小: 2048000 bytes
本地檔案時間(下載後): 2024-01-16 01:30:00
下載原因: 檔案大小不同 (本地: 1024000 bytes, 遠端: 2048000 bytes)、遠端檔案較新
狀態: 下載成功
========================================
```

#### 範例 3：跳過下載（檔案已是最新）

```
========================================
下載時間: 2024-01-16 03:20:45
遠端檔案時間: 2024-01-16 01:30:00
遠端檔案大小: 2048000 bytes
本地檔案時間(下載前): 2024-01-16 01:30:00
本地檔案大小(下載前): 2048000 bytes
狀態: 跳過下載
下載原因: 本地檔案已是最新
========================================
```

## Log 資訊說明

### 記錄欄位

| 欄位 | 說明 | 何時出現 |
|------|------|----------|
| 下載時間 | 執行下載動作的時間 | 總是出現 |
| 遠端檔案時間 | FTPS 伺服器上的檔案修改時間 | 伺服器支援時 |
| 遠端檔案大小 | FTPS 伺服器上的檔案大小 | 伺服器支援時 |
| 本地檔案時間(下載前) | 下載前本地檔案的修改時間 | 檔案已存在時 |
| 本地檔案大小(下載前) | 下載前本地檔案的大小 | 檔案已存在時 |
| 下載位元組數 | 實際下載的位元組數 | 執行下載時 |
| 最終檔案大小 | 下載完成後的檔案大小 | 執行下載時 |
| 本地檔案時間(下載後) | 下載後本地檔案的修改時間 | 執行下載時 |
| 下載原因 | 為何需要下載或跳過 | 總是出現 |
| 狀態 | 下載成功或跳過下載 | 總是出現 |

### 下載原因類型

1. **本地檔案不存在**：首次下載
2. **檔案大小不同**：遠端與本地檔案大小不一致
3. **遠端檔案較新**：遠端檔案修改時間較本地新
4. **檔案大小不同、遠端檔案較新**：同時符合兩個條件
5. **本地檔案已是最新**：不需要下載

## 使用情境

### 情境 1：除錯檔案下載問題

當發現某個檔案下載有問題時，可以檢視該檔案的 `.log` 檔案，了解：
- 遠端檔案的時間與大小
- 下載前本地檔案的狀態
- 下載過程是否成功
- 為什麼觸發下載

### 情境 2：追蹤檔案更新歷史

透過 log 檔案的歷史記錄，可以追蹤：
- 檔案何時被更新
- 每次更新前後的大小變化
- 遠端檔案的修改時間變化

### 情境 3：驗證時間戳記同步

確認下載後的本地檔案時間是否正確設定為遠端檔案時間：
```
遠端檔案時間: 2024-01-15 10:20:30
本地檔案時間(下載後): 2024-01-15 10:20:30  ✓ 一致
```

## 注意事項

### 檔案累積

- Log 檔案採用**附加模式**，不會覆蓋舊記錄
- 長期執行會累積大量歷史記錄
- 建議定期清理或歸檔舊的 log 檔案

### 磁碟空間

- 每個檔案約產生 300-500 bytes 的 log 記錄
- 如果監控模式每 30 分鐘檢查一次，每天約產生 48 筆記錄
- 100 個檔案運行一個月約產生 144,000 筆記錄（約 50-70 MB）

### 效能影響

- 寫入 log 檔案的效能影響微乎其微
- 如果寫入失敗，只會顯示警告，不會中斷下載

## 範例設定檔

### 完整設定範例

```properties
# 基本連線設定
host=ftp.example.com
port=21
user=myuser
pass=mypass

# 目錄設定
local_dir=./downloads
log_dir=./logs

# 啟用獨立檔案 log
separate_file_log=true

# 監控模式設定
monitor_mode=true
check_interval=30
allowed_time_range=02:00-05:00

# 下載檔案設定
file_names.0.remote_path=/data
file_names.0.include_prefixes.0=TCD
file_names.0.include_prefixes.1=TSC
```

## 與主 Log 的關係

啟用獨立檔案 log 時：
- **主 log**：仍會記錄程式執行的整體資訊（連線、錯誤、統計等）
- **檔案 log**：只記錄該檔案的詳細下載資訊

兩者互補，主 log 看整體流程，檔案 log 看個別檔案細節。

## 實際操作範例

### 啟用功能

1. 編輯 `config.properties`：
```properties
separate_file_log=true
```

2. 執行下載：
```batch
ftps-downloader.exe
```

3. 檢視結果：
```
logs/
├── ftps-downloader.log      ← 主 log
├── RYM01.log                 ← 檔案 log
├── RYM02.log                 ← 檔案 log
├── TCD5920.log               ← 檔案 log
└── TSC5920.log               ← 檔案 log
```

### 檢視特定檔案的下載記錄

```batch
type logs\RYM01.log
```

或使用文字編輯器開啟 `logs\RYM01.log`

## 常見問題

### Q1：啟用此功能會影響下載速度嗎？

不會。寫入 log 檔案的時間非常短暫，幾乎不影響下載效能。

### Q2：如果 log 目錄不存在會怎樣？

程式會自動建立 log 目錄。

### Q3：如果寫入 log 失敗會怎樣？

只會顯示警告訊息，不會中斷檔案下載，確保下載流程順利進行。

### Q4：可以只記錄特定檔案的 log 嗎？

目前是全部記錄。如果只想查看特定檔案，可以直接開啟對應的 `.log` 檔案。

### Q5：多久需要清理一次 log 檔案？

建議每月或每季檢視一次，將舊的 log 檔案移到備份目錄或刪除。

## 相關功能

- [監控模式](MONITOR_MODE_GUIDE.md)：自動定期檢查並下載
- [Log 功能](LOG_FEATURES_GUIDE.md)：主 log 的詳細說明
- [檔案時間同步](README.md#檔案時間戳記)：下載後設定為遠端時間

## 總結

獨立檔案 log 功能提供了更細緻的下載追蹤能力，特別適合：
- 需要精確追蹤每個檔案下載狀態的場景
- 除錯特定檔案的下載問題
- 驗證檔案時間戳記同步是否正確
- 保留檔案更新的歷史記錄

透過這個功能，可以更清楚地了解每個檔案的下載過程和狀態變化。
