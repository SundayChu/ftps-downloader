# FTPS 下載器 - 自動清理與單一實例功能說明

## 功能概述

下載器現在具備與上傳器相同的自動管理功能：

1. **日誌自動清理** - 啟動時自動刪除超過 3 天的舊日誌
2. **單一執行實例控制** - 確保同時只有一個下載程式在執行

---

## 🗑️ 日誌自動清理功能

### 功能說明

程式每次啟動時會自動檢查日誌目錄，刪除超過 **3 天**的舊日誌檔案，保持日誌目錄整潔。

### 清理規則

- **保留天數**: 3 天（可透過 `log_retention_days` 設定）
- **清理對象**: 所有 `.log` 檔案
- **判斷依據**: 檔案的修改時間 (ModTime)
- **執行時機**: 程式啟動時，設置完日誌輸出後自動執行

### 日誌範例

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【日誌清理】檢查日誌目錄: D:\logs
保留天數: 3 天 (刪除 2026-03-02 之前的日誌)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🗑️  已刪除: ftps-downloader-2026-02-28.log (修改時間: 2026-02-28 23:59:58, 大小: 325.8 KB)
🗑️  已刪除: ftps-downloader-2026-03-01.log (修改時間: 2026-03-01 18:30:22, 大小: 189.2 KB)
✓ 清理完成: 刪除 2 個舊日誌檔案，釋放空間 515.0 KB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 設定參數

在設定檔中配置：

```properties
# config.properties
log_dir=D:\logs

# 日誌保留天數（選用，預設為 3 天）
log_retention_days=3
```

---

## 🔒 單一執行實例控制功能

### 功能說明

確保同一時間只有一個下載程式在執行。如果偵測到已有舊的執行實例，會自動終止舊實例後啟動新程式。

### 運作機制

#### 1. 使用 Windows Mutex 機制
- 建立名為 `Global\FTPSDownloader_Mutex_Singleton` 的 Mutex
- 如果 Mutex 已存在，表示有其他實例正在執行

#### 2. 自動終止舊實例
- 使用 Windows `tasklist` 命令查找所有 `ftps-downloader.exe` 程式
- 使用 `taskkill /F` 強制終止舊的執行實例
- 排除當前程式自己 (透過 PID 比對)

#### 3. 安全機制
- 終止舊實例後等待 2 秒，確保舊程式完全結束
- 記錄所有操作到日誌中

### 日誌範例

#### 情境 1: 偵測到並終止舊實例

```
========================================
FTPS Downloader started
========================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【執行實例檢查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  偵測到已有執行中的下載程式實例
正在終止舊的執行實例...
🔫 終止程式 PID: 9876
✓ 已終止 PID: 9876
✓ 舊實例已終止，繼續執行新程式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### 情境 2: 沒有其他實例

```
========================================
FTPS Downloader started
========================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【執行實例檢查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ 沒有其他執行中的實例，程式正常啟動
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 應用場景

#### 1. 手動重啟
不需要手動關閉舊程式：

```bash
# 直接執行即可，舊程式會自動被終止
ftps-downloader.exe
```

#### 2. 排程重啟
在 Windows 工作排程器中設定定期重啟：

```batch
REM 每天重啟一次，自動終止舊的執行中程式
ftps-downloader.exe -config config.properties
```

#### 3. 監控模式重啟
即使程式在監控模式下執行，也可以直接重啟：

```batch
REM 舊的監控程式會被自動終止
run_ftps_downloader.bat
```

---

## 🎯 完整執行流程

程式啟動後的執行順序：

```
1. 輸出啟動訊息
   ↓
2. 【單一實例檢查】
   ├─ 偵測是否有舊實例
   ├─ 如果有 → 終止舊實例
   └─ 等待 2 秒
   ↓
3. 解析命令列參數
   ↓
4. 載入設定檔
   ↓
5. 建立本地目錄和日誌目錄
   ↓
6. 設置日誌輸出
   ↓
7. 【日誌清理】
   ├─ 掃描日誌目錄
   ├─ 刪除超過 3 天的檔案
   └─ 記錄清理結果
   ↓
8. 執行參數驗證
   ↓
9. 開始下載作業
```

---

## 📋 完整日誌範例

```
========================================
FTPS Downloader started
========================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【執行實例檢查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  偵測到已有執行中的下載程式實例
正在終止舊的執行實例...
🔫 終止程式 PID: 9876
✓ 已終止 PID: 9876
✓ 舊實例已終止，繼續執行新程式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Logging to D:\logs\ftps-downloader-2026-03-05.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【日誌清理】檢查日誌目錄: D:\logs
保留天數: 3 天 (刪除 2026-03-02 之前的日誌)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🗑️  已刪除: ftps-downloader-2026-02-28.log (修改時間: 2026-02-28 23:59:58, 大小: 325.8 KB)
🗑️  已刪除: ftps-downloader-2026-03-01.log (修改時間: 2026-03-01 18:30:22, 大小: 189.2 KB)
✓ 清理完成: 刪除 2 個舊日誌檔案，釋放空間 515.0 KB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【參數驗證】檢查設定檔參數...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ FTP 主機: ftp.example.com
✓ FTP 埠號: 990
✓ FTP 帳號: myuser
✓ FTP 密碼: ********
✓ 本地目錄: D:\downloads
✓ 遠端目錄: /data/export
✓ 監控模式: 已啟用 (間隔: 30 分鐘)
✓ TLS 模式: Implicit TLS
✓ 參數驗證通過
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

=== 啟動全天監控模式 ===
... (後續下載作業日誌)
```

---

## ⚙️ 技術細節

### 單一實例控制實作

使用 Windows Mutex API：

```go
func ensureSingleInstance() error {
    mutexName := "Global\\FTPSDownloader_Mutex_Singleton"
    ret, _, _ := procCreateMutex.Call(...)
    
    if GetLastError() == ERROR_ALREADY_EXISTS {
        // 終止舊實例
        killExistingDownloaderProcess()
        time.Sleep(2 * time.Second)
    }
    
    return nil
}
```

終止程式使用：

```go
func killExistingDownloaderProcess() error {
    // 1. 使用 tasklist 查找所有 ftps-downloader.exe
    cmd := exec.Command("tasklist", "/FI", "IMAGENAME eq ftps-downloader.exe", ...)
    
    // 2. 解析 PID
    // 3. 使用 taskkill /F /PID <pid> 終止程式
    killCmd := exec.Command("taskkill", "/F", "/PID", ...)
}
```

### 日誌清理實作

```go
func cleanOldLogs(logDir string, keepDays int) error {
    cutoffTime := time.Now().AddDate(0, 0, -keepDays)
    
    // 掃描日誌目錄
    entries, _ := os.ReadDir(logDir)
    
    for _, entry := range entries {
        if entry.IsDir() || !strings.HasSuffix(entry.Name(), ".log") {
            continue
        }
        
        info, _ := entry.Info()
        if info.ModTime().Before(cutoffTime) {
            os.Remove(filepath.Join(logDir, entry.Name()))
        }
    }
}
```

---

## 🔧 故障排除

### 問題 1: 無法終止舊實例

**現象**: 
```
❌ 無法終止舊實例: ...
請手動關閉其他執行中的下載程式後重試
```

**解決方法**:
1. 開啟工作管理員 (Ctrl+Shift+Esc)
2. 找到 `ftps-downloader.exe` 程式
3. 手動結束該程式
4. 重新執行

### 問題 2: 日誌清理失敗

**現象**:
```
⚠️  無法刪除: ftps-downloader-2026-02-28.log (存取被拒)
```

**可能原因**:
- 日誌檔案正被其他程式開啟
- 沒有刪除權限

**解決方法**:
- 確保沒有其他程式開啟該日誌檔案
- 檢查檔案權限

---

## 🎯 最佳實踐

### 1. 監控模式定期重啟
透過 Windows 工作排程器，設定每天重啟一次：

```
觸發程序: 每天 00:00
動作: 啟動程式 - ftps-downloader.exe
參數: -config config.properties
```

優點：
- ✅ 自動清理舊的日誌檔案
- ✅ 確保程式處於最新狀態
- ✅ 避免長期執行可能產生的問題

### 2. 設定合適的 log_dir
建議設定專用的日誌目錄：

```properties
log_dir=D:\ftps-downloader-logs
log_retention_days=3
```

### 3. 定期檢查日誌
- 每週檢查一次日誌目錄大小
- 確認清理功能正常運作
- 如有異常立即處理

---

## 📚 相關文件

- [README.md](README.md) - 主要功能說明
- [MONITOR_MODE_GUIDE.md](MONITOR_MODE_GUIDE.md) - 監控模式說明
- [DOWNLOADER_FILTER_GUIDE.md](DOWNLOADER_FILTER_GUIDE.md) - 下載篩選功能
- [SEPARATE_FILE_LOG_GUIDE.md](SEPARATE_FILE_LOG_GUIDE.md) - 獨立檔案日誌
- [UPLOADER_AUTO_CLEANUP_GUIDE.md](UPLOADER_AUTO_CLEANUP_GUIDE.md) - 上傳器自動清理功能

---

## 📝 更新記錄

### 2026-03-05
- ✨ 新增日誌自動清理功能（保留 3 天）
- ✨ 新增單一執行實例控制功能
- 🔧 優化程式啟動流程
- 📝 新增本說明文件
