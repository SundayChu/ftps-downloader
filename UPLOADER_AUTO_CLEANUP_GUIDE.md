# FTPS 上傳器 - 自動清理與單一實例功能說明

## 功能概述

本版本新增了兩個重要的自動管理功能：

1. **日誌自動清理** - 啟動時自動刪除超過 3 天的舊日誌
2. **單一執行實例控制** - 確保同時只有一個上傳程式在執行

---

## 🗑️ 日誌自動清理功能

### 功能說明

程式每次啟動時會自動檢查日誌目錄，刪除超過 **3 天**的舊日誌檔案，保持日誌目錄整潔。

### 清理規則

- **保留天數**: 3 天（從當前日期往前計算）
- **清理對象**: 所有 `.log` 檔案
- **判斷依據**: 檔案的修改時間 (ModTime)
- **執行時機**: 程式啟動時自動執行

### 日誌範例

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【日誌清理】檢查日誌目錄: D:\logs
保留天數: 3 天 (刪除 2026-03-02 之前的日誌)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🗑️  已刪除: ftps-uploader-2026-02-28.log (修改時間: 2026-02-28 23:59:58, 大小: 125.3 KB)
🗑️  已刪除: ftps-uploader-2026-03-01.log (修改時間: 2026-03-01 18:30:22, 大小: 89.7 KB)
✓ 清理完成: 刪除 2 個舊日誌檔案，釋放空間 215.0 KB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 無需清理時的日誌

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【日誌清理】檢查日誌目錄: D:\logs
保留天數: 3 天 (刪除 2026-03-02 之前的日誌)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ 無需清理: 沒有超過 3 天的日誌檔案
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 設定需求

需要在設定檔中配置 `log_dir` 參數：

```properties
# config.uploader.properties
log_dir=D:\logs
```

---

## 🔒 單一執行實例控制功能

### 功能說明

確保同一時間只有一個上傳程式在執行。如果偵測到已有舊的執行實例，會自動終止舊實例後啟動新程式。

### 運作機制

#### 1. 使用 Windows Mutex 機制
- 建立名為 `Global\FTPSUploader_Mutex_Singleton` 的 Mutex
- 如果 Mutex 已存在，表示有其他實例正在執行

#### 2. 自動終止舊實例
- 使用 Windows `tasklist` 命令查找所有 `ftps-uploader.exe` 程式
- 使用 `taskkill /F` 強制終止舊的執行實例
- 排除當前程式自己 (透過 PID 比對)

#### 3. 安全機制
- 終止舊實例後等待 2 秒，確保舊程式完全結束
- 記錄所有操作到日誌中

### 日誌範例

#### 情境 1: 偵測到並終止舊實例

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【執行實例檢查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  偵測到已有執行中的上傳程式實例
正在終止舊的執行實例...
🔫 終止程式 PID: 12345
✓ 已終止 PID: 12345
✓ 舊實例已終止，繼續執行新程式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### 情境 2: 沒有其他實例

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【執行實例檢查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ 沒有其他執行中的實例，程式正常啟動
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 應用場景

#### 1. 手動重啟
當您需要重新啟動上傳程式時，不需要手動去工作管理員關閉舊程式：

```bash
# 直接執行即可，舊程式會自動被終止
ftps-uploader.exe
```

#### 2. 排程重啟
在 Windows 工作排程器中設定定期重啟時，不用擔心會啟動多個實例：

```batch
REM 每天重啟一次，自動終止舊的執行中程式
ftps-uploader.exe -config config.uploader.properties
```

#### 3. 監控模式重啟
即使程式在監控模式下執行，也可以直接重啟：

```batch
REM 舊的監控程式會被自動終止
run_ftps_uploader.bat
```

---

## 🎯 完整執行流程

程式啟動後的執行順序：

```
1. 載入設定檔
   ↓
2. 建立日誌目錄
   ↓
3. 開始寫入日誌
   ↓
4. 【單一實例檢查】
   ├─ 偵測是否有舊實例
   ├─ 如果有 → 終止舊實例
   └─ 等待 2 秒
   ↓
5. 【日誌清理】
   ├─ 掃描日誌目錄
   ├─ 刪除超過 3 天的檔案
   └─ 記錄清理結果
   ↓
6. 執行參數驗證
   ↓
7. 開始上傳作業
```

---

## 📋 完整日誌範例

```
========================================
FTPS Uploader started
Config file: config.uploader.properties
========================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【執行實例檢查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  偵測到已有執行中的上傳程式實例
正在終止舊的執行實例...
🔫 終止程式 PID: 12345
✓ 已終止 PID: 12345
✓ 舊實例已終止，繼續執行新程式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【日誌清理】檢查日誌目錄: D:\logs
保留天數: 3 天 (刪除 2026-03-02 之前的日誌)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🗑️  已刪除: ftps-uploader-2026-02-28.log (修改時間: 2026-02-28 23:59:58, 大小: 125.3 KB)
🗑️  已刪除: ftps-uploader-2026-03-01.log (修改時間: 2026-03-01 18:30:22, 大小: 89.7 KB)
✓ 清理完成: 刪除 2 個舊日誌檔案，釋放空間 215.0 KB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【參數驗證】檢查設定檔參數...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ FTP 主機: ftp.example.com
✓ FTP 埠號: 990
✓ FTP 帳號: myuser
✓ FTP 密碼: ********
✓ 本地目錄: D:\uploads
✓ 遠端目錄: /data/incoming
✓ 監控模式: 已啟用 (間隔: 30 分鐘)
✓ 時間範圍: 02:00-05:00
✓ 停止時間: 18:00
✓ TLS 模式: Implicit TLS (通常使用 port 990)
✓ 上傳模式: 指定檔案清單 (2 個路徑)
✓ 參數驗證通過
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

=== 啟動全天監控模式 ===
上傳時間範圍: 02:00-05:00
檢查間隔: 30 分鐘
自動停止時間: 18:00
按 Ctrl+C 停止監控

... (後續上傳作業日誌)
```

---

## ⚙️ 技術細節

### 日誌清理實作

```go
func cleanupOldLogs(logDir string, retentionDays int) {
    // 計算臨界時間
    cutoffTime := time.Now().AddDate(0, 0, -retentionDays)
    
    // 掃描日誌目錄
    entries, _ := os.ReadDir(logDir)
    
    for _, entry := range entries {
        if entry.IsDir() || !strings.HasSuffix(entry.Name(), ".log") {
            continue
        }
        
        info, _ := entry.Info()
        if info.ModTime().Before(cutoffTime) {
            os.Remove(filepath.Join(logDir, entry.Name()))
        }
    }
}
```

### 單一實例檢查實作

使用 Windows Mutex API：

```go
func ensureSingleInstance() error {
    mutexName := "Global\\FTPSUploader_Mutex_Singleton"
    ret, _, _ := procCreateMutex.Call(...)
    
    if GetLastError() == ERROR_ALREADY_EXISTS {
        // 終止舊實例
        killExistingProcess()
        time.Sleep(2 * time.Second)
    }
    
    return nil
}
```

終止程式使用：

```go
func killExistingProcess() error {
    // 1. 使用 tasklist 查找所有 ftps-uploader.exe
    cmd := exec.Command("tasklist", "/FI", "IMAGENAME eq ftps-uploader.exe", ...)
    
    // 2. 解析 PID
    // 3. 使用 taskkill /F /PID <pid> 終止程式
    killCmd := exec.Command("taskkill", "/F", "/PID", ...)
}
```

---

## 🔧 故障排除

### 問題 1: 無法終止舊實例

**現象**: 
```
❌ 無法終止舊實例: ...
請手動關閉其他執行中的上傳程式後重試
```

**解決方法**:
1. 開啟工作管理員 (Ctrl+Shift+Esc)
2. 找到 `ftps-uploader.exe` 程式
3. 手動結束該程式
4. 重新執行

### 問題 2: 日誌清理失敗

**現象**:
```
⚠️  無法刪除: ftps-uploader-2026-02-28.log (存取被拒)
```

**可能原因**:
- 日誌檔案正被其他程式開啟
- 沒有刪除權限

**解決方法**:
- 確保沒有其他程式（如記事本）開啟該日誌檔案
- 檢查檔案權限

### 問題 3: Mutex 建立失敗

**現象**:
```
❌ 執行實例檢查失敗: 建立 Mutex 失敗
```

**可能原因**:
- 權限不足

**解決方法**:
- 以系統管理員身分執行程式

---

## 🎯 最佳實踐

### 1. 監控模式定期重啟
透過 Windows 工作排程器，設定每天重啟一次：

```
觸發程序: 每天 00:00
動作: 啟動程式 - ftps-uploader.exe
參數: -config config.uploader.properties
```

這樣可以：
- ✅ 自動清理舊的日誌檔案
- ✅ 確保程式處於最新狀態
- ✅ 避免長期執行可能產生的記憶體問題

### 2. 設定合適的 log_dir
建議設定專用的日誌目錄：

```properties
log_dir=D:\ftps-uploader-logs
```

優點：
- 便於備份和管理
- 不會與其他程式的日誌混雜
- 自動清理只影響該目錄

### 3. 定期檢查日誌
雖然會自動清理，但建議：
- 每週檢查一次日誌目錄大小
- 確認清理功能正常運作
- 如有異常立即處理

---

## 📚 相關文件

- [README.md](README.md) - 主要功能說明
- [UPLOADER_FILTER_GUIDE.md](UPLOADER_FILTER_GUIDE.md) - 上傳篩選功能
- [PREFIX_RENAME_GUIDE.md](PREFIX_RENAME_GUIDE.md) - 檔案重命名功能
- [MONITOR_MODE_GUIDE.md](MONITOR_MODE_GUIDE.md) - 監控模式說明

---

## 📝 更新記錄

### 2026-03-05
- ✨ 新增日誌自動清理功能（保留 3 天）
- ✨ 新增單一執行實例控制功能
- 🔧 優化程式啟動流程
- 📝 新增本說明文件
