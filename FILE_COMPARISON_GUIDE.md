# 檔案差異比對設定指南

## 概述

程式現在支援三種檔案比對策略，可透過設定檔控制下載行為。

## 設定選項

### 1. `force_download` - 強制下載模式

**功能**：忽略本地檔案狀態，每次都重新下載

**使用時機**：
- 需要確保檔案絕對是最新版本
- 本地檔案可能已損壞
- 測試或重置環境

**設定範例**：
```properties
# 強制每次都下載，忽略本地檔案
force_download=true
```

**優先級**：最高（會覆蓋其他所有選項）

---

### 2. `skip_if_exists` - 存在即跳過模式

**功能**：只要本地檔案存在就跳過下載，不檢查差異

**使用時機**：
- 只想下載缺少的檔案
- 本地檔案已手動更新，不想被覆蓋
- 減少網路傳輸和處理時間

**設定範例**：
```properties
# 本地檔案存在就不下載
skip_if_exists=true
```

**優先級**：中（次於 force_download）

---

### 3. `compare_by_modtime` - 純時間比對模式

**功能**：只使用修改時間比對，不檢查檔案大小（預設會雙重檢查）

**使用時機**：
- 信任 FTP 伺服器的時間戳記完全準確
- 檔案大小可能相同但需要依據時間判斷
- 需要提高檢查速度（省略大小比對）

**設定範例**：
```properties
# 只用時間比對，不檢查大小
compare_by_modtime=true
```

**優先級**：低（影響預設模式的檢查方式）

**注意事項**：
- 需要 FTP 伺服器支援 LIST 命令並提供時間資訊
- 如果遠端不提供時間，會退回到大小比對

---

## 預設行為（不設定任何選項）⭐

**預設策略：修改時間優先 + 檔案大小雙重檢查**

```properties
skip_if_exists=false
compare_by_modtime=false
force_download=false
```

**檢查流程：**

1. **優先取得並比對修改時間**
   - ✅ 遠端檔案較新 → 下載
   - 🔍 時間相同或本地較新 → 繼續檢查大小

2. **雙重檢查：比對檔案大小**
   - ✅ 大小不同 → 下載
   - ⊘ 大小相同 → 跳過

3. **無法取得時間資訊時**
   - 退而求其次使用檔案大小比對

**特點**：
- 🎯 最精確：同時檢查時間和大小
- 🛡️ 最安全：雙重檢查避免遺漏更新
- 📊 能偵測到時間相同但內容不同的檔案

---

## 決策流程圖

```
開始檢查檔案
    ↓
本地檔案是否存在？
    ├─ 否 → 下載
    └─ 是 → 繼續檢查
         ↓
    force_download=true？
         ├─ 是 → 下載（強制）
         └─ 否 → 繼續檢查
              ↓
         skip_if_exists=true？
              ├─ 是 → 跳過
              └─ 否 → 繼續檢查
                   ↓
              取得遠端檔案資訊（優先用 LIST 取得時間+大小）
                   ↓
              是否取得修改時間？
                   ├─ 是 → 比對修改時間
                   │        ├─ 遠端較新 → 下載
                   │        └─ 時間相同或本地較新
                   │             ↓
                   │        compare_by_modtime=true？
                   │             ├─ 是 → 跳過（純時間模式）
                   │             └─ 否 → 比對大小（雙重檢查）
                   │                      ├─ 大小不同 → 下載
                   │                      └─ 大小相同 → 跳過
                   │
                   └─ 否 → 是否取得檔案大小？
                            ├─ 是 → 比對大小
                            │        ├─ 大小不同 → 下載
                            │        └─ 大小相同 → 跳過
                            └─ 否 → 跳過（無法取得資訊，保留本地）
```

---

## 實際應用範例

### 範例 1：標準同步模式（預設，推薦）⭐

```properties
# 不設定或設為 false（預設值）
skip_if_exists=false
compare_by_modtime=false
force_download=false
```

**行為**：
- 優先使用修改時間判斷
- 時間相同時再檢查大小（雙重保險）
- 無時間資訊時退而求其次用大小比對

**結果**：最精確的同步，能偵測所有差異

---

### 範例 2：快速檢查模式

```properties
# 只下載缺少的檔案，已存在的全部跳過
skip_if_exists=true
```

**結果**：執行速度最快，適合大量檔案的初次下載

---

### 範例 3：純時間比對模式

```properties
# 只用時間判斷，不檢查大小（提高速度）
compare_by_modtime=true
skip_if_exists=false
```

**結果**：信任時間戳記，省略大小檢查，適合檔案量大且時間準確的場景

---

### 範例 4：完全重新下載

```properties
# 強制重新下載所有檔案
force_download=true
```

**結果**：所有檔案都會重新下載，適合環境重置或故障排除

---

## 日誌輸出說明

不同設定會產生不同的日誌訊息：

### 預設模式（修改時間 + 大小雙重檢查）⭐
```
  🔎 比對修改時間
     遠端: 2026-01-24 10:30:00
     本地: 2026-01-23 15:20:00
  ⚠️  遠端檔案較新 (相差 19h10m0s)
  ➜  下載原因: 遠端檔案較新，需要更新 (時間差: 19h10m0s)
```

或時間相同但大小不同：
```
  🔎 比對修改時間
     遠端: 2026-01-24 10:30:00
     本地: 2026-01-24 10:30:00
  🔎 時間相同，進一步檢查檔案大小
  ⚠️  檔案大小不同 (遠端: 12345 bytes, 本地: 12340 bytes)
  ➜  下載原因: 檔案大小不同，需要更新
```

### 統計結果輸出

成功下載檔案時會列出檔案清單：
```
════════════════════════════════════════════════
📊 本次檢查結果統計:
════════════════════════════════════════════════
  🔎 檢查檔案總數: 5 個
  ✅ 新下載檔案: 3 個

  📥 下載的檔案列表:
     1. TCD5920240124.txt
     2. TSC5920240124.txt
     3. DATA20240124.csv
  ⊘  檔案未更新 (已跳過): 2 個
════════════════════════════════════════════════
```

無新檔案時：
```
════════════════════════════════════════════════
📊 本次檢查結果統計:
════════════════════════════════════════════════
  🔎 檢查檔案總數: 5 個
  ℹ️  新下載檔案: 0 個 (無新檔)
  ⊘  檔案未更新 (已跳過): 5 個
════════════════════════════════════════════════
```

### 純時間比對模式 (compare_by_modtime=true)
```
  🔎 比對修改時間
     遠端: 2026-01-24 10:30:00
     本地: 2026-01-24 10:30:00
  ✓  檔案修改時間相同（純時間比對模式）
  ⊘  跳過下載: 檔案修改時間相同，無需下載
```

### 強制下載模式
```
  🔄 強制下載模式
  ➜  下載原因: 設定為強制下載模式，忽略本地檔案
```

### 存在即跳過模式
```
  ℹ️  本地檔案已存在 (12345 bytes)
  ⊘  跳過下載: 本地檔案已存在，跳過下載 (大小: 12345 bytes)
```

### 無時間資訊時的大小比對模式
```
  ℹ️  無法取得修改時間，改用檔案大小比對
  🔎 比對檔案大小 (遠端: 12345 bytes, 本地: 12345 bytes)
  ✓  檔案大小相同
  ⊘  跳過下載: 檔案一致，無需下載 (大小: 12345 bytes)
```

---

## 中文編碼支援

程式已內建完整的中文編碼支援：

### Windows 系統
- ✅ 自動支援 UTF-8 和 ANSI (Big5/CP950) 編碼
- ✅ 日誌輸出自動適應控制台編碼
- ✅ Go 1.6+ 在 Windows 上自動處理 UTF-8 輸出
- ✅ 支援中文檔案名稱和路徑

### 日誌檔案
- 日誌檔案統一使用 UTF-8 編碼
- 可使用支援 UTF-8 的編輯器開啟（VS Code, Notepad++ 等）
- Windows 記事本 (Windows 10+) 也可正確顯示

### 測試方式
執行程式後檢查：
1. 控制台輸出是否正確顯示中文
2. 日誌檔案 (.log) 是否正確記錄中文

---

## 命令列參數覆蓋

這些選項也可以透過命令列參數臨時覆蓋：

```bash
# 強制下載
ftps-downloader.exe -config config.properties -force-download

# 存在即跳過
ftps-downloader.exe -config config.properties -skip-if-exists

# 使用時間比對
ftps-downloader.exe -config config.properties -compare-by-modtime
```

---

## 常見問題

### Q: 預設行為是什麼？

A: **預設使用修改時間優先 + 檔案大小雙重檢查**
- 優先取得並比對修改時間，遠端較新就下載
- 時間相同時進一步檢查檔案大小，大小不同也下載
- 無法取得時間時退而求其次用大小比對
- 這是最精確和安全的策略

### Q: 可以同時設定多個選項嗎？

A: 可以，但有優先級：
1. `force_download` 優先級最高
2. `skip_if_exists` 次之
3. `compare_by_modtime` 只影響比對方式（是否雙重檢查大小）

### Q: FTP 伺服器不支援時間資訊怎麼辦？

A: 程式會自動退回到檔案大小比對模式，不會影響運作。

### Q: compare_by_modtime=true 和預設模式有什麼差別？

A: 
- **預設模式**（false）：時間相同時還會檢查大小（雙重保險）
- **pure 模式**（true）：只看時間，時間相同就跳過（不檢查大小）

### Q: 監控模式下建議使用哪種設定？

A: 建議使用**預設設定**（不設任何選項），這樣能最精確地偵測檔案變化。避免使用 `force_download`（會每次都重新下載所有檔案）。

### Q: 如何確認檔案有被更新？

A: 查看日誌中的 `✅ 新下載檔案` 統計，或檢查 `logs/` 目錄中的詳細日誌。

### Q: 為什麼有時候時間相同但還是下載了？

A: 因為預設模式會進行雙重檢查，如果時間相同但檔案大小不同，仍會下載。這能避免遺漏真正有變化的檔案。

---

## 版本資訊

- **建立日期**：2026-01-24
- **適用版本**：v1.0+
- **相關檔案**：`main.go`, `types.go`, `config.properties`
